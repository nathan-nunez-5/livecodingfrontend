For this task you may use both the PBE window to leverage synthesis, as well the code window on the left to manually write code.
</br></br>
Write a function which takes a single string parameter as input.
If the length of the input string is divisible by 3,
return a string constructed by taking every third character of the input.
If the length of the input is not divisible by 3, return the input string's first and last letter concatenated together.
