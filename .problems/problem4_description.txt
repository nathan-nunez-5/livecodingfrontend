For this task you may use both the PBE window to leverage synthesis, as well the code window on the right to manually write code.
</br></br>
Write a function that takes two strings parameters as input.
The function should output a string of length 2 by concatenating the fifth characters of each string.
If either or both of the string inputs do not have a fifth character, the function should use an underscore ("_") character instead.
