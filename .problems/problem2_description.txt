For this task you may use both the PBE window to leverage synthesis, as well the code window on the left to manually write code.
</br></br>
Write a function f that takes a single integer parameter as input.
If the input is 0, return the string literal "nothing".
Otherwise, the function should return a value 2 more than 3 times the input.
